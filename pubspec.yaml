name: whatbytes
description: "Smart task management for gig workers - Your gigs, organized. Your time, optimized."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  permission_handler: ^11.3.1

  
  # State Management - Riverpod (Beginner-friendly!)
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.5.0
  
  # Utilities
  intl: ^0.20.2              # Date formatting
  equatable: ^2.0.7          # Value comparison
  
  # UI
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1       # For Poppins font

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^6.0.0
  
  # Code generation (optional for Riverpod)
  build_runner: ^2.4.14
  riverpod_generator: ^2.5.0
  
  # App Icon Generator
  flutter_launcher_icons: ^0.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/burnmate_logo.png
    - assets/burnamte_notification.png
    - assets/burnmate_notification_large.png
    - assets/burnmate_notification_badged.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Launcher Icons Configuration
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/burnmate_logo.png"
  adaptive_icon_background: "#0F172A"  # Premium dark background
  adaptive_icon_foreground: "assets/burnmate_logo.png"
  # see https://flutter.dev/to/font-from-package
